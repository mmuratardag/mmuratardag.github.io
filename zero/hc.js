// TODO: Fix this
// UPDATE: Won't fix this
// UPDATE 2: Can't fix this
// UPDATE 3: Nobody knows what this does anymore
// UPDATE 4: Works on my machine ¯\_(ツ)_/¯
function mysteryFunction(x) {
  return x * 42 + Math.random() - Math.random();
}